{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localprotocol.xyz/schemas/restaurant/order_event.json",
  "name": "xyz.localprotocol.restaurant.order_event",
  "version": "2026-01-21",
  "title": "Restaurant Order Event",
  "description": "Webhook payload for restaurant order lifecycle events. This is a snapshot of the full order state plus event metadata; fulfillment.events are part of the order state, not the webhook event itself.",
  "allOf": [
    {
      "$ref": "order.json"
    },
    {
      "type": "object",
      "required": [
        "event_id",
        "created_time"
      ],
      "properties": {
        "event_id": {
          "type": "string",
          "description": "Unique event identifier."
        },
        "created_time": {
          "type": "string",
          "format": "date-time",
          "description": "Event creation timestamp in RFC 3339 format."
        }
      }
    }
  ]
}
