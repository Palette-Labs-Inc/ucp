{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://xyz.localprotocol/schemas/delivery/types/retail_location.json",
  "title": "Retail Location Response",
  "description": "A pickup location (retail store, locker, etc.).",
  "type": "object",
  "required": [
    "id",
    "name"
  ],
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique location identifier."
    },
    "name": {
      "type": "string",
      "description": "Location name (e.g., store name)."
    },
    "address": {
      "$ref": "postal_address.json",
      "description": "Physical address of the location."
    },
    "location": {
      "$ref": "location.json",
      "description": "Geographic coordinates for this location."
    }
  }
}
