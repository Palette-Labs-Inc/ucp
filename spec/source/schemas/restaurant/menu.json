{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localprotocol.xyz/schemas/restaurant/menu.json",
  "name": "xyz.localprotocol.restaurant.menu",
  "version": "2026-01-11",
  "title": "Menu Capability",
  "description": "Menu discovery and configurable menu item definitions.",
  "type": "object",
  "$defs": {
    "menu_item": {
      "$ref": "types/item.json"
    },
    "category": {
      "$ref": "types/category.json"
    },
    "context": {
      "allOf": [
        { "$ref": "../shopping/types/context.json" },
        {
          "type": "object",
          "properties": {
            "intent": {
              "type": "string",
              "description": "Background context for semantic search (e.g., 'quick lunch nearby', 'family meal under $30')."
            }
          }
        }
      ],
      "description": "Location and market context for menu operations. Extends base context with 'intent' for semantic search."
    },
    "search_request": {
      "type": "object",
      "required": ["query"],
      "properties": {
        "query": {
          "type": "string",
          "description": "Free-text search query."
        },
        "context": {
          "$ref": "#/$defs/context"
        },
        "pagination": {
          "$ref": "../shopping/types/pagination.json#/$defs/request"
        }
      }
    },
    "search_response": {
      "type": "object",
      "required": ["ucp", "categories"],
      "properties": {
        "ucp": {
          "$ref": "../ucp.json#/$defs/response"
        },
        "categories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/category"
          },
          "description": "Menu categories with nested items."
        },
        "pagination": {
          "$ref": "../shopping/types/pagination.json#/$defs/response"
        },
        "messages": {
          "type": "array",
          "items": {
            "$ref": "../shopping/types/message.json"
          },
          "description": "Errors, warnings, or informational messages about the search results."
        }
      }
    },
    "get_item_response": {
      "type": "object",
      "required": ["ucp", "item"],
      "properties": {
        "ucp": {
          "$ref": "../ucp.json#/$defs/response"
        },
        "item": {
          "$ref": "types/item.json",
          "description": "The requested menu item."
        },
        "messages": {
          "type": "array",
          "items": {
            "$ref": "../shopping/types/message.json"
          },
          "description": "Errors, warnings, or informational messages about the requested item."
        }
      }
    },
    "get_item_modifier_groups_response": {
      "type": "object",
      "required": ["ucp", "modifier_groups"],
      "properties": {
        "ucp": {
          "$ref": "../ucp.json#/$defs/response"
        },
        "modifier_groups": {
          "type": "array",
          "items": {
            "$ref": "types/modifier_group.json"
          },
          "description": "Modifier groups available for the item."
        },
        "messages": {
          "type": "array",
          "items": {
            "$ref": "../shopping/types/message.json"
          },
          "description": "Errors, warnings, or informational messages about the requested item."
        }
      }
    },
    "get_modifier_item_response": {
      "type": "object",
      "required": ["ucp", "modifier_item"],
      "properties": {
        "ucp": {
          "$ref": "../ucp.json#/$defs/response"
        },
        "modifier_item": {
          "$ref": "types/modifier_item.json",
          "description": "The requested modifier item."
        },
        "messages": {
          "type": "array",
          "items": {
            "$ref": "../shopping/types/message.json"
          },
          "description": "Errors, warnings, or informational messages about the requested modifier."
        }
      }
    },
    "list_response": {
      "type": "object",
      "required": ["ucp", "categories"],
      "properties": {
        "ucp": {
          "$ref": "../ucp.json#/$defs/response"
        },
        "categories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/category"
          },
          "description": "Menu categories with nested items."
        },
        "messages": {
          "type": "array",
          "items": {
            "$ref": "../shopping/types/message.json"
          },
          "description": "Errors, warnings, or informational messages about the menu."
        }
      }
    }
  }
}
