{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localprotocol.xyz/schemas/commerce/types/modifier_item_selection.json",
  "title": "Menu Modifier Item Selection",
  "description": "Selected modifier item and nested selections within a modifier group.",
  "type": "object",
  "required": ["item_id"],
  "properties": {
    "item_id": {
      "type": "string",
      "description": "Selected modifier item identifier."
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Selected quantity for this modifier option."
    },
    "child_selections": {
      "type": "array",
      "items": {
        "$ref": "modifier_selection.json"
      },
      "description": "Nested modifier selections triggered by this option."
    }
  }
}
