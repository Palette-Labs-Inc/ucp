{
  "name": "ucp-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "engines": {
    "pnpm": "9.12.2"
  },
  "devDependencies": {
    "@indexsupply/shovel-config": "catalog:",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  },
  "scripts": {
    "generate:ts": "node spec/generate_ts_schema_types.js",
    "generate:contracts": "pnpm -C packages/contracts generate:abis && pnpm -C packages/contracts generate:addresses"
  }
}
